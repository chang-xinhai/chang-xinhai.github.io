name: Get Citation Data

on:
  workflow_dispatch:
  schedule:
    # Refresh Scholar data every day at 08:00 UTC. GitHub Pages serves the generated JSON from the google-scholar-stats branch.
    - cron: '0 8 * * *'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r google_scholar_crawler/requirements.txt
      - name: Crawl Google Scholar
        id: crawl
        run: |
          set +e
          cd google_scholar_crawler
          timeout 5m python main.py
          status=$?
          if [ "$status" -eq 0 ]; then
            echo "updated=true" >> "$GITHUB_OUTPUT"
          else
            echo "updated=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Google Scholar crawl failed or timed out; keeping previous published citation data."
          fi
          exit 0
        env:
          GOOGLE_SCHOLAR_ID: ${{ secrets.GOOGLE_SCHOLAR_ID }}
          FILL_PUBLICATION_DETAILS: "false"
      - name: Publish citation data
        if: steps.crawl.outputs.updated == 'true'
        run: |
          cd google_scholar_crawler/results
          git init
          git config --local user.name "${GITHUB_ACTOR}"
          git config --local user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          git add *.json
          git commit -m "Updated Citation Data" || exit 0
          git push "https://${GITHUB_ACTOR}:${{ secrets.GITHUB_TOKEN }}@github.com/${GITHUB_REPOSITORY}.git" HEAD:google-scholar-stats --force
